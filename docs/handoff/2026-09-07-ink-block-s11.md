# 잉크 & 블록 restyle — session 11 handoff (P8 done; initiative closed; device QA still open)

## Goal of the session and current status

P8 is done and the 잉크 & 블록 restyle initiative is **closed on paper**: `docs/adr/0020-ink-and-block-restyle.md`
records the 2026-09-03 pastel baseline (shipped without an ADR) and the 2026-09-06 ink-and-block baseline with PO
decisions E1–E11, the routine calls, the literal / substitution exception table and the tests moved per session;
`DESIGN.md` is rewritten for the new system; `docs/DEVELOPMENT_STATUS.md` (top section), `design-reference/README.md`
and `CLAUDE.md` (§1-1 plans line, §3 gallery count, §5-1, §5-3, §5-4, §11 C-2) are synchronized and `AGENTS.md`
regenerated; both `docs/plans/` files are deleted; `LfPromiseSeam` (no consumer since P6-C) and the dead CSS
(LEGACY `.lf-pinky*` / `.lf-pinky-badge` / `.sl-*` block plus the P3 transitional `.lf-status-dot` /
`.lf-chip--cream` / `.lf-avatar-button`) are removed with the web copy kept byte-identical. **The acceptance web was
deployed this session** (`firebase deploy --only hosting:web` after a fresh `build:web` of the P7 markup →
`https://littlefinger-app.web.app`, 18 files, release complete). **The device QA ran this session** on the PO's phone (SM-N981N, debug dev client + Metro, 360×800 dp via
density 480, font scale 1.0 and 1.5): every reachable screen matches the gallery grammar and nothing clips —
details in the verification section. `app.json` splash / notification colours were re-derived from the tokens
(PO delegated the call), and the PO reported the AdMob app-ads.txt approval.

## Files created/modified (paths)

- `docs/adr/0020-ink-and-block-restyle.md` (new), `DESIGN.md` (rewritten), `docs/DEVELOPMENT_STATUS.md` (top section
  → "complete", gates, open items), `design-reference/README.md` (baseline, counts, bundle row), `CLAUDE.md` +
  `AGENTS.md` (sync), `docs/notes/environment-gotchas.md` (new section: web CSS changes need jest).
- Deleted: `docs/plans/2026-09-03-pastel-sticker-restyle.md`, `docs/plans/2026-09-06-ink-block-restyle.md`,
  `apps/mobile/src/components/LfPromiseSeam.tsx`, `docs/handoff/2026-09-07-ink-block-s10.md`.
- `apps/mobile/src/components/components.test.tsx`: Seam import, the two Seam assertions and the `<LfPromiseSeam />`
  render removed (tests renamed). `apps/mobile/src/screens/scr-a05-promise-detail.test.tsx`: the stale
  `queryByTestId('promise-seam')` negative assertion removed.
- `design-reference/styles/components.css` → `apps/web/src/styles/components.css` (copied, `cmp` identical): LEGACY
  block (55 lines) + `.lf-avatar-button` + `.lf-chip--cream` + `.lf-status-dot*` deleted. `--lf-status-dot` the token
  stays (179 frozen; `.lf-list-item__dot` still reads it).
- `apps/mobile/src/theme/tokens.test.ts`: the `base.css` lockstep assertion moved — web `base.css` must start with the
  reference byte-for-byte and the remainder must begin with the `/* WEB ONLY */` banner; `screens/web.css` stays exact.
- Memory: `po-ink-block-restyle-decisions` (status), `ink-sticker-baseline` + `pastel-sticker-restyle-decisions`
  (superseded notes), `MEMORY.md` index, `metro-windows-launch-recipe` (the phone now carries the debug client).
- Second pass: `apps/mobile/app.json` (splash `#F3ECDC` → `#FBF8F1`, notification `#FFE59A` → `#FFD43B`;
  adaptive-icon field unchanged), `apps/mobile/config/firebase-config.test.js` (assertions moved), `DESIGN.md` brand
  section + ADR 0020 consequences (the app.json paragraph), `docs/DEVELOPMENT_STATUS.md` (device QA, AdMob approval),
  `docs/notes/environment-gotchas.md` (session restore on reinstall).

## Decisions made + why

- **The `base.css` assertion was failing since P7 and nobody knew.** Session 10 appended the WEB ONLY section, ran
  Vitest and skipped jest ("no `apps/mobile` file changed") — but the lockstep test is jest-expo. Moved, not
  loosened: prefix identity + banner check implements exactly the s10 decision. Recorded in `environment-gotchas.md`.
- Dead CSS was removed on grep evidence (no `class="…sl-…"`, no bare `lf-pinky`, no `lf-status-dot` /
  `lf-chip--cream` / `lf-avatar-button` in `design-reference/screens`, `index.html`, `apps/web/src`, `apps/mobile/src`)
  rather than the handoff's suggested `npm run preview` eyeball — grep is deterministic and covers all 42 pages.
- CLAUDE.md §11 C-2 was still "Default: no — Expo MaterialIcons" although the Symbols subset closed it on
  2026-09-03; corrected while syncing §5-4. §1-1's stale monetization-plan sentence replaced with the empty-directory
  state.
- DESIGN.md keeps the E-1 brand-mark section and the older decision-log entries (trimmed, not deleted) so the
  visual history stays readable in one file; ADR 0020 carries the detail.
- Not pushed: `origin/main` has none of the restyle commits (local `main` is 29+ ahead since `1169b3c`). Pushing is
  outward-facing; left to the PO / user.

## Verification state (what passed, what did not)

`npm run typecheck` exit 0 · `npx vitest run` **114 files / 2,179 tests** · `cd apps/mobile && npx jest` **84 suites / 928 tests** ·
`npm run build:web` ✓ (`main-*.js` 616 kB, pre-existing chunk warning) · `npm run check:agents` ✓ · `git diff --check`
✓ · reference ↔ web `components.css` `cmp` identical · `firebase deploy --only hosting:web` → release complete.
**Device QA** (SM-N981N `R3CN80ZJ8SV`, `app-debug.apk` of 2026-09-07 07:19 + Metro 8143, `wm density 480` →
360×800 dp, font scale 1.0 then 1.5; density and font scale restored): A00, A02 home (진행 중 · 대기 · 지난 약속
entry), the menu sheet, A03 step 1, A04 (needs-issue state), A05 PENDING and CHECKING (two promises), A06, A07,
A08, A09, MOD-02 — each captured beside its gallery page (`.playwright-mcp/captures/gallery/`, montages in the
session scratchpad). Every screen follows the gallery grammar; Android `boxShadow` renders the 5 / 3 px ink
shadows including the stamp with its clipped corner blob (the P5 concern is closed); at 1.5 nothing clips — CTA
labels wrap and the buttons grow, the hero title wraps. `npx jest config/firebase-config.test.js` 6 / 6 after the
colour move. **Not verified:** the deployed site in a real KakaoTalk in-app browser; A05 ACTIVE / COMPLETED /
BROKEN / DISPUTED, MOD-01 / 03 / 04 / 05 and SCR-I (the account has no ACTIVE promise — gallery-confirmed only);
the new splash / notification colours (need a native build).

## Blocked / PO-confirmation items

- **Copy decision resolved (2026-09-07, ADR 0021):** one language per locale, with compact Korean
  replacements approved by the PO. The app, acceptance web and gallery now use the revised copy.
- Cosmetic follow-ups seen on device (not fixed): the A09 filter chips wrap to two rows at 360 dp (the gallery
  squeezes four chips into one row with a 4 px right gutter); the A04 stamp corner blob sits top-left in RN
  (`LfStamp` has one corner) while the gallery A04 uses `lf-blob--corner-right`; A05 CHECKING before the user
  answers shows the compact stamp, not the gallery's pink RESPONSE countdown card (the gallery draws the answered state).
- The phone now carries the **debug dev client** (no Littlefinger package was installed when the session started);
  reinstall from Play when the PO wants the release build back. The signed-in session came back on the fresh
  install (apparently Android auto-backup, `allowBackup=true`) — no login was needed.
- Carried: AdMob app-ads.txt is **approved (PO, 2026-09-07)**. Still before any production-track promotion: one real
  UMP choice/re-open and one real rewarded SSV grant on the Play code 24 test installation. No further refunds, no
  European message republish.

## The exact next step

No restyle step remains and the device QA is done. Next: whatever the PO asks — the natural candidates are the
remaining cosmetic follow-ups and the AdMob UMP / SSV evidence. The copy decision is resolved in ADR 0021. To drive the phone again:
`cd apps/mobile && CI=1 EXPO_NO_TYPESCRIPT_SETUP=1 npx expo start --dev-client --port 8143`, `adb -s R3CN80ZJ8SV
reverse tcp:8143 tcp:8143`, `adb -s R3CN80ZJ8SV shell am start -a android.intent.action.VIEW -d
"littlefinger://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8143"`, dismiss the dev menu, `wm density 480`
for 360×800 dp (`wm density reset` after), `settings put system font_scale 1.5` (restore 1.0), capture with
`exec-out screencap -p`; tap with `input swipe x y x y 80` and **capture before every tap** — blind coordinate
sequences drifted twice this session (a tap on the trust strip opened 마이, a second Back left the app). Gallery
pairs: Playwright `page.$('.lf-device__viewport').screenshot()` against `npm run preview`, side-by-side with PIL.
Shell: `cd /c/DEV/littlefinger` at the start of every call (the cwd persists — `npm run …` from `apps/mobile`
fails); jest from `apps/mobile`; **multi-line edits = python script written to the scratchpad and run with
`python <path>`** (a heredoc mangles backslash sequences and cannot contain a backtick); `PYTHONIOENCODING=utf-8`;
the Edit tool needs a prior Read of that file.
