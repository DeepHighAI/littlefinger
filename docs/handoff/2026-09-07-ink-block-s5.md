# 잉크 & 블록 restyle — session 5 handoff (batch 2 done, checkpoint 2 requested)

## Goal of the session and current status

Checkpoint 1 (revised bundle, 29 pairs) was **confirmed by the PO on 2026-09-06** ("컨펌. 배치 2 진행해"). This
session finished **P4 batch 2**: the 13 expansion screens that have no artboard were rewritten to the block grammar and
checked screen by screen against the README component rules with a computed-style rule table (`_compare/spec.js`),
then the whole 42-frame gallery was run through the same table. **Checkpoint 2 is requested; P5 must not start before
the PO confirms.** Ahead: P5–P6 RN components/screens, P7 web markup (deployment hold lifted after), P8 docs/ADR 0020.
Plan: `docs/plans/2026-09-06-ink-block-restyle.md` (§2 E1–E11, Phase D–F).

Commits on `main` this session: batch-2 commit `feat: re-align gallery batch 2 to the ink-and-block grammar` and the
handoff commit that follows. Earlier: `755cac6` bundle adoption, `3004a52` grammar, batch-1 re-alignment, s4 handoff.

## Files created/modified (paths)

- Markup (13): `design-reference/screens/app/scr-a05-active-no-end.html`, `scr-a05-declined.html`,
  `scr-a05-finish-pending.html`, `scr-a05-unresolved.html`, `mod-01-amend-request-no-end.html`,
  `mod-05-entitlement-sheet-locked.html`, `scr-blocked-users.html`, `scr-profile-nickname.html`, `scr-not-found.html`,
  `scr-update-required.html`; `design-reference/screens/web/scr-w04-participant-view.html`,
  `scr-w04-participant-view-finish.html`, `scr-w05-witness-confirm-no-end.html`.
- `design-reference/styles/components.css` (+ byte copy `apps/web/src/styles/components.css`): 48dp touch expansion
  extended to `.lf-segmented__item::after`, `.lf-btn--tonal::after` (+ `position: relative`), `.lf-proof__remove::after`,
  `.lf-switch::before` (the switch's `::after` is the knob).
- `design-reference/styles/screens/app-entry.css`: `.lf-home__checking-cta` keeps the artboard's 44h but gets
  `position: relative` + `::after` 48 touch box.
- `design-reference/styles/screens/web.css` (+ byte copy): `.lf-response-btn` 44h/min 48 r10 2px paper 600 +
  pressed yellow 800 3px, `.lf-attach-btn` dashed, `.lf-dashed--field`.
- Review harness, **untracked, delete after checkpoint 2**: `design-reference/_compare/` (`index.html`, `pair.html`,
  `single.html`, `measure.js`, `spec.js`, `artboard/`), scratchpad `extract-artboards.js` / `phase-*.py`. Captures:
  `.playwright-mcp/cp1/` (29 pairs) and `.playwright-mcp/cp2/` (13 singles), gitignored.

## Decisions made + why

- No new PO decision was needed in batch 2; every value came from the README table or a batch-1 precedent.
- **48dp wins over component height** (§8-7, same call as the artboard's 44h "수정 제안" in batch 1): segmented items
  38h, tonal 36h, proof remove 22, switch 32 and the home CHECKING CTA 44 all keep their README/artboard size and
  expand the touch box with a pseudo-element. RN must mirror this with `hitSlop` in P5 (`LfSegmented`, `LfButton`
  tonal, `LfProof` remove, `LfSwitch`, home CHECKING CTA).
- W04's action pair is the README CTA pair (`lf-btn--filled lf-btn--cta` + `lf-btn--outlined lf-btn--cta`, both
  `--grow`) instead of the tonal/compact pair the old page used — tonal is a 36h in-card action, not a screen CTA.
- Browser-bar buttons (`.lf-browserbar .lf-icon-button`, 40px borderless) are OS chrome, not product UI, and are
  excluded from the icon-button rule.
- Batch-2 art: `scr-not-found` = `lf-oval--web lf-oval--muted` + `lf-eyes--web`, `scr-update-required` =
  `lf-oval--web` + eyes, nickname = yellow card with `lf-oval--hint`; they no longer use the 240×211 home blob.
- Three remaining rule-table rows on A02 home are **intentional artboard variants confirmed at checkpoint 1**: the
  CHECKING row CTA (canvas bg, r10, 44h) and the in-feed ad chip (24h, eyebrow). Not defects.

## Verification state (what passed, what did not)

`npm run typecheck` (5 projects) exit 0 · `npx vitest run` **114 files / 2,179 tests** 통과 (the first background run
reported 1 failed test whose name the filtered log did not keep; the immediate full rerun passed 2,179/2,179 — treat as
a flaky timing test, not a regression) · `cd apps/mobile && npx jest` **84 suites / 916 tests** 통과 ·
`npm run build:web` 통과 · `git diff --check` 통과. Gallery audit (Playwright, 42 iframes, 434 rule checks, 35 images):
0 horizontal overflow, 0 broken images, console 0 errors / 0 warnings, 0 touch-target violations, 0 4-colour-face
text violations, disclaimer verbatim on every `.lf-disclaimer`. Batch-2 rule table: all 13 pages 0 violations.

Not done: device run (P5), RN/web/docs phases.

## Blocked / PO-confirmation items

- **체크포인트 2 컨펌 대기 — 컨펌 없이 P5 금지.** Live harness: `npm run preview` then
  `http://localhost:4173/_compare/single.html?p=app/<file>.html` (`window.lfCheck()` returns the rule table) or
  `pair.html?i=0..28` for batch 1. All untracked.
- Carried: AdMob app/app-ads.txt recheck after 2026-09-06 23:15 KST (investigate if still absent after 2026-09-12
  23:15 KST), then one real UMP choice/re-open and one real rewarded SSV grant before any production-track promotion.
  No further refunds, no European message republish. **Web deployment on hold until P7.** `app.json`
  splash/notification colours out of scope unless asked.

## The exact next step

**After confirmation**: delete `design-reference/_compare/` and the scratchpad scripts (nothing to commit — untracked),
then P5 per `docs/plans/2026-09-06-ink-block-restyle.md` Phase E: `LfAppBar` r14 + oval mascot tile, `LfButton`
filled = yellow/ink + paper trailing, `LfFab` yellow, `LfHero` paper arrow, `LfStatusTile`, home menu sheet, `LfBlob`
oval variants, `LfAvatar` md 44, `LfSegmented` 38, press literals in `Pressable` style functions, `hitSlop` for the
five 48dp cases above. Freeze list unchanged (`packages/shared`, `supabase`, `apps/mobile/src/lib`, `*-state.ts`,
`_layout.tsx`, app config); no role/label/testID snapshot changes. Then P6 screens, P7 web markup, P8 docs.
Shell: `cd /c/DEV/littlefinger` first in every call; jest from `apps/mobile`; multi-line edits via Write or a
`PYTHONIOENCODING=utf-8 python - <<'PY'` heredoc writing with `newline='\n'` (Python text mode otherwise leaves CRLF);
Playwright MCP writes only under `.playwright-mcp/` (gitignored).
