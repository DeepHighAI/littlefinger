# 잉크 & 블록 restyle — session 8 handoff (P6-B done; device check still login-blocked)

## Goal of the session and current status

P6-B is implemented and committed (`b52f75b`): `promise/edit.tsx` (A03), `invite.tsx` (A04),
`fulfillment/[promise_id].tsx` (A06), `completion-celebration-sheet.tsx` (MOD-03), `slot-paywall-sheet.tsx`
(MOD-04), plus the four component additions they needed. All runners green. **No device capture this
session**: the emulator dev client (Metro attached on 8143) is still on the login screen and every dev email
test account is rejected server-side, so A03/A04/A06 and both sheets remain unverified on device, together
with the P6-A leftovers (A02 home, menu sheet, A08, stamp shadow in `overflow: hidden`, 1.5 font scale).
Ahead: P6-C, P7 web markup (deployment hold lifted after), P8 docs/ADR 0020.
Plan: `docs/plans/2026-09-06-ink-block-restyle.md` (§2 E1–E11, Phase E–F).

Commits on `main` this session: `b52f75b` P6-B screens, plus this handoff (s7 deleted) with the gotcha note.

## Files created/modified (paths)

- `apps/mobile/src/app/promise/edit.tsx`: literals `WIZARD_TOP` 18 / `BODY_TOP` 18; wizard padding 18/20/0,
  body 18/20/24/16 gap 16; step 1 = one shadowed `LfCard`, step 2 = conditions card (end date + keeper, hint
  `meta`) + reward/penalty card; choices gap 6; app bar chip = `LABEL.editing` ("작성 중") yellow status;
  actions bar = paper background, no hairline, row with 임시저장 text link left and right-aligned CTA
  (`trailing` arrow_forward / send); review rows divided by dashed 2px outline.
- `apps/mobile/src/screens/promise-edit-labels.ts`: `stepCount` removed from ko/en (the wizard label already
  says "1/3 · 내용").
- `apps/mobile/src/app/invite.tsx`: body 22/20/20/16 gap 14; `LfStamp` gets `body`; Kakao share
  (`variant="kakao"`, 3px, `KakaoMark` leading) + outlined copy; chat bubble (2px ink, r 4/14/14/14, 12 14
  padding, `label` title, dashed-divided `note` "약속 확인하기"); countdown card = yellow `LfStatusTile`
  schedule + eyebrow + `countdown` text + 10h pill progress (2px ink, muted, sky fill, progressbar role) +
  `meta` notice; waiting chip paper; needs-issue card uses a paper `link_off` tile; witness invite outlined.
- `apps/mobile/src/app/fulfillment/[promise_id].tsx`: literals `BODY_TOP` 22, `RADIO_SIZE` 24, `RADIO_DOT` 12,
  `PROOF_ICON` 24, `REMOVE_SIZE` 22, `REMOVE_ICON` 14, `PARTNER_ICON` 20; summary = muted flat card
  (`bodyStrong` + `meta`); question left-aligned `title` + `bodySm` secondary; answer tiles = paper r14 2.5 ink
  with mint check / pink close `LfStatusTile`, `stamp` title, `disclaimer` subtitle, 24 ring radio (dot when
  selected), selected = yellow + 5px and `LfInkContext`; proof thumbs 84 r12 2px + 3px (image carries the
  inner radius, tile no longer clips), dashed add tile without shadow, 22 ink remove/retry circles with
  hitSlop 13, filenames `eyebrow`; comment counter and hint `meta`; "상대의 확인을 기다리고 있습니다" =
  flat paper card with pending `LfAvatar` "?" + `note` + hourglass 20; result chip `statusToneOf` + `kind
  status`; claim chips paper on both sides; CTA right-aligned with `check` tile.
- `apps/mobile/src/components/completion-celebration-sheet.tsx`: `LfSheet centered` with `art` =
  `LfOval celebrate` + `LfPinkyLoop eyes solid spark`; copy `bodySm` secondary; stat chip 34h r8 yellow 2px +
  3px (`STAT_ICON` 16, `note` text); CTA `trailing="add"`; share = text link.
- `apps/mobile/src/components/slot-paywall-sheet.tsx`: pink notice box (2px ink r10, `note`), bookmark 20 +
  `bodyStrong` usage, `caption` rule, yellow `LfCard` offer (`stamp` + `note`), CTA `trailing="arrow_forward"`.
- `apps/mobile/src/components/LfText.tsx`: `titleHeavy` (24/30/900 tight) and `note` (13/19/800 ink).
- `apps/mobile/src/components/LfSheet.tsx`: `art` prop; `centered` renders floating close (top 10 / right 18),
  art, then the title as `titleHeavy` — no title row.
- `apps/mobile/src/components/LfStamp.tsx`: `body` prop (12.5/18/500 secondary, `.lf-stamp__body`).
- `apps/mobile/src/components/LfButton.tsx`: `kakao` now 3px (pressed none); new `kakaoLogin` keeps 5px
  (pressed 2px). `app/index.tsx` uses `kakaoLogin`.
- `apps/mobile/src/components/KakaoMark.tsx` (new): 18px speech-bubble path in `colors.onKakao`.
- Tests moved: `mod-03-completion-celebration.test.tsx` (pinky testID read with `includeHiddenElements`
  because the oval is a11y-hidden like the artboard), `components.test.tsx` (variants list + kakao 3px /
  kakaoLogin 5px).
- `docs/notes/environment-gotchas.md`: new section — the agent Bash tool cannot run a heredoc containing a
  backtick; write scripts to the scratchpad and run `python <path>`.

## Decisions made + why

- A03 app bar shows the DRAFT status chip ("작성 중", yellow) as the gallery does; the step count was a
  duplicate of the wizard label, so `stepCount` left the catalog. Routine, reported as 통보.
- A03 step 2 keeps reward/penalty as inputs (the wizard is a PO decision; the gallery's outcome stickers are
  the filled state) but puts them in their own card so the conditions card matches the artboard.
- A04 preview bubble has no sender avatar: the screen has no nickname source without a new lib call
  (`apps/mobile/src/lib/**` is frozen) and the a04 test forbids the partner name on this screen.
- A06 keeps the comment textarea (spec F-07) although the artboard omits it; the "상대방은 아직 답하지
  않았어요" flat row is applied to the existing `waiting` copy rather than adding a label.
- A06 claim chips are paper for both parties (README: DISPUTED 종이 칩, P1) instead of yellow.
- MOD-03 keeps an accessibility close button (test + a11y) floating at the handle row; the artboard has none.
- MOD-04 follows the gallery (yellow offer, CTA outside). The sky offer with inner 3px CTA is MOD-05 → P6-C.
- Proof thumbnails no longer use `overflow: hidden` on the shadowed tile; the image gets the inner radius, so
  the 3px `boxShadow` cannot be clipped (same concern as the stamp — still unverified on device).

## Verification state (what passed, what did not)

`npm run typecheck` exit 0 · `cd apps/mobile && npx jest` **84 suites / 928 tests** 통과 · `npx vitest run`
**114 files / 2,179 tests** 통과 · `git diff --check` 통과. Device: **nothing captured** — emulator-5554 is on
A01 with "테스트 로그인에 실패했습니다" (test accounts retired); Metro `CI=1` on 8143 is running
(`packager-status:running`). **Not verified on device:** A02 home, menu sheet, A03, A04, A06, A08, MOD-03,
MOD-04, stamp/thumb `boxShadow` inside clipping, 1.5 font scale.

## Blocked / PO-confirmation items

- **로그인 필요:** to run the device check the PO signs in with Kakao/Google on the emulator (or re-provisions
  a dev test account), then captures A02/A03/A04/A06/A08 + both sheets
  (`$LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe -s emulator-5554 exec-out screencap -p > file.png`).
- Carried: AdMob app/app-ads.txt recheck after 2026-09-06 23:15 KST (investigate if still absent after
  2026-09-12 23:15 KST), then one real UMP choice/re-open and one real rewarded SSV grant before any
  production-track promotion. No further refunds, no European message republish. **Web deployment on hold until
  P7.** `app.json` splash/notification colours out of scope unless asked.

## The exact next step

P6-C per the plan: `apps/mobile/src/app/promise/[promise_id].tsx` (all 9+ variants; remove the `'-0.8deg'`
literals at lines 153 and 165; D-Day 56 r14 yellow square; header status chip via `statusToneOf`; DISPUTED
both cards paper, same size), `components/promise-amend-sheet.tsx` (MOD-01 + no-end), `witness-invite-sheet.tsx`
(MOD-02), `promise-entitlement-sheet.tsx` (MOD-05 + locked: `--sky` offer with inner 3px CTA), `app/i/[token].tsx`
(SCR-I invite review, disclaimer verbatim), and the support screens `blocked-users.tsx`, `profile-nickname.tsx`,
`+not-found.tsx`, `update-required.tsx` (last two already have the oval from P6-A — only spacing/copy check).
Gallery: `design-reference/screens/app/scr-a05-*.html` (11 files), `mod-01-*.html`, `mod-02-witness-invite.html`,
`mod-05-*.html`, `scr-i-invite-review.html`, `scr-blocked-users.html`, `scr-profile-nickname.html`; CSS in
`styles/screens/app-detail.css` (A05 · SCR-I) and `app-support.css` (§MOD-01/02/05 blocks already read this session: `.lf-hint`, `.lf-witness*`,
`.lf-slot-sheet__offer--sky`). Move text assertions only. Then P7, P8. Restart Metro with the recipe in memory
`metro-windows-launch-recipe`; relaunch the client with `adb -s emulator-5554 shell am start -a
android.intent.action.VIEW -d "littlefinger://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8143"`.
Shell: `cd /c/DEV/littlefinger` first; jest from `apps/mobile`; **multi-line edits = python script written to
the scratchpad and run with `python <path>`** (a heredoc with any backtick fails to parse); `PYTHONIOENCODING=utf-8`.
