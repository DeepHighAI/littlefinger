# 잉크 & 블록 restyle — session 4 handoff (revised bundle adopted, batch 1 re-aligned, checkpoint 1 re-requested)

## Goal of the session and current status

The PO replaced the first handoff with `design-reference/design_handoff_ink_block/` and ruled it final ("이전 번들은
무시, 새 번들이 확정"). This session adopted that bundle (tokens 179, old bundle deleted), moved the component grammar
to it (r14 header, yellow CTA/FAB with paper trailing square, paper hero arrow, irregular-oval mascots, block Google,
kakao login 5px), and re-aligned all 27 batch-1 gallery pages **one screen at a time** against the new artboards with
a computed-style diff table per screen (PO working rule). **Checkpoint 1 is re-requested with 29 capture pairs; P5
must not start before the PO confirms.** Ahead: P4 batch 2 (13 screens, E6), P5–P6 RN, P7 web markup, P8 docs/ADR.
Plan: `docs/plans/2026-09-06-ink-block-restyle.md` (§2 E1–E11).

Commits on `main` this session: `755cac6` adopt the revised bundle (tokens 179, old bundle removed, press literals),
`3004a52` component grammar, and the batch-1 re-alignment commit that follows this file. Earlier: `437c353` batch 1
(first bundle), `5d52250` s3 handoff.

## Files created/modified (paths)

- Bundle: `design-reference/design_handoff_ink_block/` committed; `design-reference/redesign-2026-09-06/` removed.
- Tokens: `design-reference/styles/tokens.css` = bundle file verbatim (179); `apps/web/src/styles/tokens.css` same
  `:root`; `apps/mobile/src/theme/tokens.ts` lost `size.pressOffset`, `elevation.cardPressed/smPressed`.
- `apps/mobile/src/theme/tokens.test.ts`: count 179, press assertions removed, notice/caption/proof/trust-note
  assertions moved to the new contract (not relaxed).
- `design-reference/styles/components.css` (+ byte copy `apps/web/src/styles/components.css`): `.lf-appbar` r14,
  `.lf-appbar__mascot` oval 36×34 (+`--lg` 38×36 for SCR-I), `.lf-oval/__inner/__outer` + `--history/--celebrate/
  --web/--muted/--hint`, `.lf-eyes--history/--web`, `.lf-blob--empty` = A00 SVG 240×211 + drop-shadow +
  `.lf-pinky-loop--half`, `.lf-blob--hero/--login` -2deg + `#FFE59A` (E11), `.lf-btn--filled` yellow/ink,
  `.lf-btn__trailing` paper, `.lf-fab` yellow (add 26), `.lf-btn--kakao-login` 5px, `.lf-btn--google` block 52h,
  `.lf-btn > .material-symbols-rounded` 18, `.lf-segmented__item` 38, `.lf-avatar--md` 44 / `--row` 36 / `--xl` glyph 20,
  `.lf-notice` no shadow + `--pink`, `.lf-caption` 13/19/600 + `--strong` 12.5/18/800, `.lf-title/.lf-headline/
  .lf-sheet__title` 800 + `.lf-title--heavy`, `.lf-title--web` 22/28/900, `.lf-compare__label` 800 + `__note` +
  icon rule, `.lf-claim` 5px + who 14/800 + time 11/600, `.lf-card__meta` 600, `.lf-list-item__supporting` 600,
  `.lf-stamp` 22/18/16 gap 6, `--compact` gap 4, `__time` 600, `--completed` approvals paper + time 800,
  `.lf-stamp__headline--lg`, `.lf-card--fill`, `.lf-card--list .lf-chevron` 18, `.lf-meta__label/__value` 13,
  `.lf-preview__hint` 600, `.lf-empty__hint` 13, `.lf-countdown` 22, `.lf-btn--tonal` 3px; press literals
  (`translate(3px,3px)`, `2px 2px 0`, `none`); dead rules removed (`.lf-blob--circle*`, `__accent`, `--tilt*`,
  `.lf-btn__trailing--plain`, `.lf-btn--google-web` emptied).
- `screens/app-entry.css` (home spacing, menu sheet avatar 18/900, hero arrow paper, hero meta 800, ad chip 24,
  onboarding CTA left), `app-create.css` (answer tiles 22, `.lf-fulfillment__promise`, proof dashed, partner icon 20,
  bubble body 12.5), `app-detail.css` (`.lf-detail__actions` outlined hug + filled grow, `.lf-response-note`,
  `.lf-dispute__title`, `.lf-witness-line`, retention row 13/800 + 12/600, `.lf-avatar--muted` ink border, icons 20),
  `app-support.css` (hint `--sky`, witness locked slot, slot sheet `--sky` + inner CTA 3px, `.lf-slot-sheet__usage`
  800 + `--sm`, trust value 800, slot row 12/16, stat-change 13), `screens/web.css` (+ byte copy: `.lf-info-banner`
  block, `.lf-avatar--alt` paper, `.lf-web-note`, 19px icon override removed, hover lift).
- Markup: 22 gallery pages touched (oval mascots on A02-empty/A09/MOD-03/W01/W03/W06, A01 oval, SCR-I tile, avatars
  `--row/--md`, `--plain` removed, W01 links `screens/web.css` (it never had), notes/captions reclassed).
- Docs: plan §1–§3 + E7–E11, `docs/DEVELOPMENT_STATUS.md` bundle path, memory `po-ink-block-restyle-decisions`.

## Decisions made + why

- PO (a)–(d) and E11 are in the plan §2 and memory. Routine calls reported as 통보 in the checkpoint message: eyebrow
  token (11) for the artboard's 10px captions; W01 headline 24/30 (26/32 not in scale); kakao label 16 (README CTA)
  though artboards vary 15/16/17; Google label 17 (artboards 15/17); 48dp touch minimum over the artboard's 44h
  "수정 제안"; A08 segmented 38 (README) vs artboard 40; A08 bottom ad slot kept (§8-1) though absent from the artboard;
  trailing icon 22 (README) where two artboards use 24/26.
- **Artboard prototypes are `content-box`**: every artboard box measures +4/+5 and the device +16 wider than the
  README numbers. The gallery (`border-box`) matches README, so those rows are not defects.
- Literal exceptions for ADR 0020 (PO chose bundle fidelity over new tokens): press 3/2/none, oval radii ×2, mascot
  tile 36×34 / 38×36 / 60×56, oval art sizes, segmented 38, avatar md 44, Google 52h, web hover 6px, eyes 60/80,
  `#FFE59A` + `-2deg` on A00/A01, stamp padding 22/18/16, ad chip 24, badge 24, wizard 18.

## Verification state (what passed, what did not)

`npm run typecheck` (5 projects) 통과 · `npx vitest run` **114 files / 2,179 tests** 통과 · `cd apps/mobile && npx jest`
**84 suites / 916 tests** 통과 · `npm run build:web` 통과 · `git diff --check`
통과. Gallery audit (Playwright, 42 iframes): 0 horizontal overflow (document/viewport/body/sheet/appbar), 0 broken
images, console 0 errors. Per-screen computed diff (`_compare/measure.js`) for the 27 batch-1 pairs shows only:
content-box artefacts, E8 weight mappings, decided 11px captions, and the 통보 items above.

Not done: device run (P5), batch 2, RN/web/docs phases. Two batch-2 pages (`scr-not-found`, `scr-update-required`)
still use `.lf-blob--empty`, which is now the 240×211 home variant — they render oversized until batch 2 rewrites them.

## Blocked / PO-confirmation items

- **체크포인트 1 (개정 번들) 컨펌 대기 — 컨펌 없이 P5 금지.** Live harness: `npm run preview` then
  `http://localhost:4173/_compare/pair.html?i=0..28` (`index.html` lists all 29, but 58 iframes make Chrome MCP time
  out — use `pair.html`). `measure.js` exposes `window.lfCompare()` on `pair.html`. All untracked; delete after checkpoint 2.
- Carried: AdMob app/app-ads.txt recheck after 2026-09-06 23:15 KST (investigate if still absent after 2026-09-12
  23:15 KST), then one real UMP choice/re-open and one real rewarded SSV grant before any production-track promotion.
  No further refunds, no European message republish. **Web deployment on hold until P7.** `app.json` splash/notification
  colours out of scope unless asked.

## The exact next step

**After confirmation**, P4 batch 2 with the same loop (implement → capture → table; no artboard, so compare against
the style guide §5 rules): `scr-a05-active-no-end`, `scr-a05-declined`, `scr-a05-finish-pending`,
`scr-a05-unresolved`, `mod-01-amend-request-no-end`, `mod-05-entitlement-sheet-locked`, `scr-blocked-users`,
`scr-profile-nickname`, `scr-not-found`, `scr-update-required`, `screens/web/scr-w04-participant-view`,
`scr-w04-participant-view-finish`, `scr-w05-witness-confirm-no-end`. Start with `scr-not-found` and
`scr-update-required` (they reuse `.lf-blob--empty` and need the `.lf-oval--web`/`--muted` art). Add each to
`_compare/index.html` ROWS only if an artboard exists; otherwise capture the gallery page alone. Then checkpoint 2,
delete `design-reference/_compare/` and the scratchpad scripts, and go to P5 (`docs/plans` §5).
Shell: `cd /c/DEV/littlefinger` first in every call (the cwd persisted into `apps/mobile` once this session and made a
verification run silently no-op); jest from `apps/mobile`; multi-line edits via Write or a `python - <<'PY'` heredoc
with `PYTHONIOENCODING=utf-8`; Playwright MCP writes only under `.playwright-mcp/` (gitignored).
