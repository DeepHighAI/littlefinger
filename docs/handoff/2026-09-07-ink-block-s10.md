# 잉크 & 블록 restyle — session 10 handoff (P7 done; web deploy hold lifted; P8 docs next)

## Goal of the session and current status

P7 is implemented and committed (`aac7d00`): the six SCR-W screens are re-marked on the ink-and-block grammar,
`index.html`/`app.html` carry theme-color `#FBF8F1` (`seo.test.ts` moved), `apps/web/src/styles/base.css` is
byte-identical to the reference above a new `/* WEB ONLY */` section (fixed screen + safe-area insets), and the
"P7 전까지" button-child segmented rule left `web.css` in both the reference and the copy. The built web was
captured 360×800 on all eight artboard routes with fixtures (no backend) — `tools/capture-web-screens.js`, output in
`.playwright-mcp/captures/` (gitignored), montage sent to the PO. **The web deployment hold from P3 is lifted**
(`docs/DEVELOPMENT_STATUS.md`); the deploy itself (`firebase deploy --only hosting:web`) waits for the PO's go.
Device check of the RN screens (P6-A/B/C) is still blocked on the emulator sign-in. Ahead: P8 docs/ADR 0020.
Plan: `docs/plans/2026-09-06-ink-block-restyle.md` (§2 E1–E11, Phase E–F).

Commits on `main` this session: `aac7d00` P7 web, plus this handoff (s9 deleted) with STATUS + gotchas.

## Files created/modified (paths)

- `apps/web/index.html`, `apps/web/app.html`: theme-color `#F3ECDC` → `#FBF8F1`; `src/seo.test.ts` expectation moved.
- `apps/web/src/styles/base.css`: reference copy + `/* WEB ONLY */` (`.lf-screen { position: fixed }`,
  `env(safe-area-inset-top/bottom)` on `--web` body/actions). `design-reference/styles/screens/web.css` and the copy:
  `.lf-segmented button` rules deleted (markup now uses `.lf-segmented__item` + `role="tab"`).
- `apps/web/src/components/LfMascot.tsx`: `LfBlob` replaced by `LfOval` (`web` 190×152 with inner paper oval,
  `hint` 60×56, `muted`); `LfEyes` gains `web`. `LfMascot.test.tsx` moved to the oval.
- `scr-w01-invite-landing.tsx`: pink countdown notice, `PinkyBadge` = web oval + solid loop (shared by W02/W03/W04
  loading), `lf-title--heavy` headline, `lf-card--fill` preview, Kakao `--kakao-login` (5px) + Google web variant
  (52h 3px, no `--cta`), `lf-web-note` caption. Test moved: `.lf-blob--login` → `.lf-oval--web`.
- `scr-w02-promise-review.tsx`: content card is the 5px sticker (no `--flat`); decline was already the text button.
- `scr-w03-approval-complete.tsx`: `lf-stamp--completed` (mint) with `lf-stamp__pill` + colour loop, 20px headline,
  approval dots, fingerprint without icon; `RevisitCard` → `RevisitActions` bottom bar (web note + filled CTA with
  arrow + Android `lf-btn-link` to /promises); the no-result state shows the oval + the same bar.
- `scr-w04-participant-promises.tsx`: pink/sky cards keep the 5px shadow, submit and amend-response buttons are 56h
  (`--cta`), info banners carry icons + `__text`, the amend sheet gets a `lf-sheet__title-row` with an icon close,
  `role="tablist"/"tab"` segments and a `send` trailing tile, the version sheet gets the same title row, the
  CHECKING pending banner an hourglass, and the list ends with `lf-web-note` (`installNote`, new ko/en label).
  Test moved: two tab queries `button` → `tab`.
- `scr-w05-witness-confirm.tsx`: parties line and sign hint are `lf-caption--strong`.
- `scr-w06-link-expired.tsx`: muted web oval + `lf-eyes--web`. Test moved: `.lf-blob--empty` → `.lf-oval--web.lf-oval--muted`.
- `tools/capture-web-screens.js` (new): Playwright MCP capture harness (route fixtures, fake session, W03 router state).
- `docs/DEVELOPMENT_STATUS.md` (hold lifted, snapshot 2026-09-07), `docs/notes/environment-gotchas.md` (capture harness).

## Decisions made + why

- W03 keeps no `.lf-app-hint` install card although the artboard has one: EC-I03 and the W03 tests forbid an
  install banner on both platforms; the app/web re-entry lives in the bottom action bar instead. Routine, 통보.
- W06 has no "앱에서 새 약속 보기" button: `02` §4-3-3 says no primary CTA on the dead-link page and the test locks
  it; the spec outranks the artboard.
- W02 keeps the creator avatar row (§4-3-4 display element) and the visible amend-field label with its validation
  hint; the artboard's sr-only label + placeholder would drop the hint.
- W04 keeps the version-history text button and the DISPUTED claim grammar (no web artboard for them); W05 keeps the
  confirmation checkbox and the danger 나가기 button (F-05).
- Segmented tabs use `tab` roles on web as on the app (A02 filter, MOD-01 precedent).
- `base.css` diverges from the reference only below the WEB ONLY marker; the device-frame rules above it stay
  because the copy must remain byte-identical for the next sync.
- Capture harness lives in `tools/` (repo root is the only Playwright-allowed root) and never calls the real
  backend; the deploy is not run — outward-facing, PO decision.

## Verification state (what passed, what did not)

`npm run typecheck` exit 0 · `npx vitest run` **114 files / 2,179 tests** 통과 (web alone 26 / 256) · `git diff
--check` 통과 · `apps/web` `npm run build` ✓ (`main-*.js` 616 kB > 500 kB chunk warning is pre-existing) · 8 captures
`ok` (`w01 w06 w02 w03 w04 w04-finish w05 w05-no-end`), eyeballed against the artboards. `npx jest` was not re-run —
no `apps/mobile` file changed this session (last run 84 / 928 in s9). **Not verified:** the deployed site (not
deployed), real KakaoTalk in-app browser safe-area behaviour, everything RN on a device.

## Blocked / PO-confirmation items

- **배포 실행 여부 (PO):** hold is lifted; `firebase deploy --only hosting:web` from `apps/web/dist` (build of
  `aac7d00`) is ready but was not run.
- **로그인 필요:** the RN device check (P6-A/B/C, every screen) still needs a Kakao/Google sign-in on the emulator.
- Carried: AdMob app/app-ads.txt recheck after 2026-09-06 23:15 KST (investigate if still absent after
  2026-09-12 23:15 KST), then one real UMP choice/re-open and one real rewarded SSV grant before any
  production-track promotion. No further refunds, no European message republish. `app.json` splash/notification
  colours out of scope unless asked.

## The exact next step

P8 per the plan: write `docs/adr/0020-ink-and-block-restyle.md` (records the 2026-09-03 pastel baseline that shipped
without an ADR and the 2026-09-06 ink-and-block baseline; PO decisions E1–E11 from `docs/plans/2026-09-06-ink-block-restyle.md`
§2 and `memory po-ink-block-restyle-decisions`; the routine decisions and deviations listed in the s8/s9/s10
handoffs — read `git log -p docs/handoff` for s8/s9; the tests-moved list; "Supersedes the visual system of ADR 0012");
rewrite `DESIGN.md` for 잉크 & 블록 (paper `#FBF8F1`, one ink, four faces, hard shadows 5/3, r14/10/8/20, Pretendard
500/600/800/900, component grammar §3-2 of the plan); update the top of `docs/DEVELOPMENT_STATUS.md`,
`design-reference/README.md`, and `CLAUDE.md` §3/§5-1/§5-3/§5-4 (잉크 & 스티커 → 잉크 & 블록, tokens.ts shape, Pretendard
weights, ADR 0020 pointer) then `npm run sync:agents` + `npm run check:agents`; delete `docs/plans/2026-09-03-*` (pastel)
and `docs/plans/2026-09-06-ink-block-restyle.md`; delete `apps/mobile/src/components/LfPromiseSeam.tsx` and its
`components.test.tsx` block (no consumer since P6-C); optional cleanup of the reference `components.css` LEGACY block
(`.lf-pinky*`, `.lf-pinky-badge`) once `npm run preview` confirms no gallery page uses them. Write the last handoff.
Shell: `cd /c/DEV/littlefinger` first; **multi-line edits = python script written to the scratchpad and run with
`python <path>`** (a heredoc works only when the script has no backtick); `PYTHONIOENCODING=utf-8`.
