# 잉크 & 블록 restyle — session 7 handoff (P6-A done; device check partial, login blocked)

## Goal of the session and current status

P6-A is implemented and committed (`8054f44`): `home.tsx`, `notifications.tsx`, `profile.tsx`, `history.tsx`,
A00/A01 art (E11), `+not-found` / `update-required` ovals; `LfBlob.tilt` removed. All runners green. The device
check ran on the emulator with a freshly built debug dev client: **A00 and A01 verified** (hard `boxShadow`,
SVG oval, disabled/enabled CTA states). **Home / profile could not be captured**: the dev email test accounts are
retired server-side (both `test3@test.com` and `test@test.com` are rejected), and a Kakao/Google login must be
typed by the PO. Ahead: P6-B, P6-C, P7 web markup (deployment hold lifted after), P8 docs/ADR 0020.
Plan: `docs/plans/2026-09-06-ink-block-restyle.md` (§2 E1–E11, Phase E–F).

Commits on `main` this session: `8054f44` P6-A screens, plus this handoff (s6 deleted).

## Files created/modified (paths)

- `apps/mobile/src/app/home.tsx`: README spacing (headline 22 / tabs 18 / body 22 as literals), section title
  removed, hero gets `badge` (needs-response text or D-Day) + `description` (end date) + `meta` (parties).
- `apps/mobile/src/components/LfHero.tsx`: `dday` → `badge`, `eyebrow` optional, margins 16/20, `arrow_forward`.
- `apps/mobile/src/components/PromiseListRow.tsx`: container margins 16/20, row gap 14.
- `apps/mobile/src/app/notifications.tsx`: rows = paper r14 2.5 ink, unread = yellow + 5px, icon tile 40 r10 2px,
  headline 14/20/800 ink, supporting 12/18/600 (ink on yellow), dot 10 ink; body 22/20/20/16 gap 20.
- `apps/mobile/src/app/profile.tsx`: avatar `xl` with yellow 3px shadow wrapper, name `cardTitle`, meta `meta`,
  trust card = paper `LfCard` (value `bodyStrong`, notes `micro`), slot row = `LfCard shadow={false}` + sky
  `LfStatusTile` + tonal 36, reminder/legal/blocked = bordered flat cards with 48h rows and dashed dividers,
  language = `LfSegmented`, icons 18. Literals `BODY_TOP` 22, `SETTINGS_ICON` 18, `AVATAR_SHADOW` 3.
- `apps/mobile/src/components/LfSegmented.tsx`: items accept `accessibilityLabel`.
- `apps/mobile/src/app/history.tsx`: `LfEmpty art="history"`.
- `apps/mobile/src/app/index.tsx`: `LfOval variant="login"` + loop `scale(0.7)` literal. `onboarding.tsx`: `tilt`
  prop dropped. `+not-found.tsx`: `LfOval web muted` + eyes 80. `update-required.tsx`: `LfOval web` + eyes 80.
- `apps/mobile/src/components/LfBlob.tsx`: `tilt` prop and `LfBlobTilt` removed.
- Tests moved: `scr-a02-home.test.tsx` (section title → selected tab state), `scr-a07-notifications.test.tsx`
  (unread headline colour = ink), `scr-a08-profile.test.tsx` (language = tabs + hitSlop), `components.test.tsx`
  (hero badge/meta, blob without tilt).
- Untracked artefacts: `.playwright-mcp/device/*.png` (captures), `apps/mobile/android/app/build/outputs/apk/
  debug/app-debug.apk` (dev client, 272 MB, gitignored).

## Decisions made + why

- Hero keeps an end-date line as `description` although the artboard shows only title + parties — the home
  tests pin the localized end date on the hero and the information matters; reported to the PO as 통보.
- The home section title ("진행 중 약속") is gone (README/artboard has none); the WAITING-tab test now checks the
  selected tab instead.
- Notification "읽지 않음" text label stays next to the dot (§8-7: never colour alone).
- Slot tile in the menu sheet and the profile slot row both use the sky `LfStatusTile` (gallery `--sky` icon).
- Dev client: `JAVA_HOME` must be Android Studio JBR 21 (`docs/notes/environment-gotchas.md`); JDK 25 fails the
  worklets CMake configure. The emulator AVD `Littlefinger_API_36_1` now has the debug client; the PO's phone
  keeps its Play release build and was not touched.

## Verification state (what passed, what did not)

`npm run typecheck` exit 0 · `cd apps/mobile && npx jest` **84 suites / 927 tests** 통과 · `git diff --check`
통과 (vitest unchanged since s6: 114 / 2,179). Device (emulator 1080×2400 @420, Metro `CI=1` port 8143):
A00 = pale-yellow blob −2° + yellow r14 CTA with 5px shadow ✓; A01 = two-layer oval with hard shadow, r8 notice,
Kakao yellow r14 + 5px / Google white 2.5 ink + 3px, disabled = opacity .3 without shadow ✓. **Not verified:**
A02 home, home menu sheet, A08 profile, the stamp's `boxShadow` inside `overflow: hidden`, 1.5 font scale.

## Blocked / PO-confirmation items

- **로그인 필요:** the emulator dev client is on the login screen with Metro attached. To finish the device
  check the PO signs in with Kakao/Google on the emulator (or re-provisions a dev test account), then captures
  A02/A08 (`adb -s emulator-5554 exec-out screencap -p > file.png`).
- Carried: AdMob app/app-ads.txt recheck after 2026-09-06 23:15 KST (investigate if still absent after 2026-09-12
  23:15 KST), then one real UMP choice/re-open and one real rewarded SSV grant before any production-track promotion.
  No further refunds, no European message republish. **Web deployment on hold until P7.** `app.json`
  splash/notification colours out of scope unless asked.

## The exact next step

P6-B per the plan: `promise/edit.tsx` (wizard bars + "1/3 · 내용" already from `LfWizardProgress`; create body
18/20/24/16 gap 16, kakao preview bubble), `invite.tsx` (`radius.pill` literals at lines ~72/78 → r14 blocks,
share sheet + copy button), `fulfillment/[promise_id].tsx` (answer tiles, proof tiles 84 r12, line ~149 pill),
`completion-celebration-sheet.tsx` (`LfOval variant="celebrate"` + loop; pills at 63/74/88), `slot-paywall-sheet.tsx`
(pills 58/72 → r10 blocks, `--sky` offer with inner 3px CTA). Compare each against
`design-reference/screens/app/scr-a03-*.html`, `scr-a04-*.html`, `scr-a06-*.html`, `mod-03-*.html`,
`mod-04-*.html` and their CSS in `styles/screens/app-create.css` / `app-support.css`. Then P6-C (detail variants,
`-0.8deg` literal removal at `promise/[promise_id].tsx:153,165`, D-Day square, sheets, `i/[token]`, support
screens), P7, P8. Restart Metro with the recipe in memory `metro-windows-launch-recipe`; relaunch the client with
`adb -s emulator-5554 shell am start -a android.intent.action.VIEW -d "littlefinger://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8143"`.
Shell: `cd /c/DEV/littlefinger` first; jest from `apps/mobile`; multi-line edits via a Write-tool script run with
`python file.py` (a bash heredoc followed by a `\|` grep fails to parse); `PYTHONIOENCODING=utf-8`.
